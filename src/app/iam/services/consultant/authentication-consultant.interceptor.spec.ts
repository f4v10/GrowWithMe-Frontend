import { AuthenticationConsultantInterceptor } from './authentication-consultant.interceptor';

describe('AuthenticationInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthenticationConsultantInterceptor()).toBeTruthy();
  });
});
