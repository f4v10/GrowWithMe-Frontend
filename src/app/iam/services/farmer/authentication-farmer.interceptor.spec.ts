import { AuthenticationFarmerInterceptor } from './authentication-farmer.interceptor';

describe('AuthenticationFarmerInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthenticationFarmerInterceptor()).toBeTruthy();
  });
});
