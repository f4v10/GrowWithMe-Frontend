import { ConsultantSignUpRequest } from './consultant-sign-up.request';

describe('ConsultantSignUpRequest', () => {
  it('should create an instance', () => {
    expect(new ConsultantSignUpRequest()).toBeTruthy();
  });
});
