import { ConsultantSignInResponse } from './consultant-sign-in.response';

describe('ConsultantSignInResponse', () => {
  it('should create an instance', () => {
    expect(new ConsultantSignInResponse()).toBeTruthy();
  });
});
