import { ConsultantSignInRequest } from './consultant-sign-in.request';

describe('ConsultantSignInRequest', () => {
  it('should create an instance', () => {
    expect(new ConsultantSignInRequest()).toBeTruthy();
  });
});
