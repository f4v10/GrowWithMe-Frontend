import { FarmerSignUpRequest } from './farmer-sign-up.request';

describe('FarmerSignUpRequest', () => {
  it('should create an instance', () => {
    expect(new FarmerSignUpRequest()).toBeTruthy();
  });
});
